<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:GigRadarMobile.Views"
       x:Class="GigRadarMobile.Shells.EOShell"
       Shell.FlyoutBehavior="Disabled"
       Title="GigRadar EO">

    <!-- GIGRADAR_ROLE_SYSTEM.md §26 — EOShell -->
    <TabBar>
        <ShellContent Title="Dashboard" ContentTemplate="{DataTemplate views:EoProfilePage}" Route="dashboard"/>
        <ShellContent Title="Events" ContentTemplate="{DataTemplate views:EoEventsPage}" Route="eo-events"/>
        <ShellContent Title="Tickets" ContentTemplate="{DataTemplate views:ManageTicketsPage}" Route="eo-tickets"/>
        <ShellContent Title="Analytics" ContentTemplate="{DataTemplate views:EoAnalyticsPage}" Route="eo-analytics"/>
        <ShellContent Title="Profile" ContentTemplate="{DataTemplate views:ProfilePage}" Route="profile"/>
    </TabBar>

</Shell>